name: Zizmor

on:
  push:
    branches: [main]
  pull_request:
  # Lets release-gate dispatchers run this on a version branch.
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  zizmor:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          persist-credentials: false
      - uses: zizmorcore/zizmor-action@3dc1ecc9bcb9e94e9b2c709687979e1298497054 # v0.6.2
        with:
          version: "1.29.0"
          # SARIF upload needs Advanced Security, which the private half of
          # the fleet does not have; plain output fails the job on findings
          # either way.
          advanced-security: false
